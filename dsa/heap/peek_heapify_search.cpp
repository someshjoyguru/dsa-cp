// Peek
// If implemented using array return arr[0]
// If implemented using binary tree return root node.

// Heapify
// A heapify operation can be used to create a
// max heap from an unsorted array.
// This is done by starting at the last non-leaf node
// and repeatedly performing the “bubble down” operation until
// all nodes satisfy the heap property.
// The time complexity of heapify in a max heap is O(n).

// Search
// linear Search O(n)